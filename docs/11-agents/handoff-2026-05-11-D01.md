# Handoff — сессия 2026-05-11 (B-02 + D-01 entity-extractor)

## Handoff

- **Сделано.**
  - **B-02 DONE:** Alembic миграция `202605110002` — 6 таблиц: `topic`, `mention`, `interaction` (партиционирована по `created_at`), `task`, `processed_event` (партиционирована по `processed_at`), `audit_log` (партиционирована по `ts`). Партиции 2026-05 и 2026-06 созданы. Применена на сервере.
  - **D-01 DONE:** `entity-extractor` работает на сервере.
    - Consumer `events.telegram.message` (group `entity-extractor`).
    - spaCy NER: `ru_core_news_sm` + `en_core_web_sm`, авто-детект языка через `langdetect`.
    - Person reconciliation через `rapidfuzz` (similarity ≥ 0.85).
    - Пишет в `mention`, `topic` при наличии известных персон.
    - Публикует `EntityFoundEvent` в `events.processing.entity_found`.
    - Idempotency через `processed_event` таблицу.
    - DLQ: `events.dlq.entity_found`.
  - `libs/contracts`: добавлены `EntityCandidate`, `EntityFoundEvent`, `STREAM_ENTITY_FOUND`, `STREAM_DLQ_ENTITY`.
  - `docker-compose.yml`: добавлен сервис `entity-extractor` (512 МБ limit — spaCy модели требуют памяти).
  - Smoke test подтверждён: первый queued message обработан, `events.processing.entity_found` содержит событие.

- **Не сделано.**
  - spaCy модели `sm` вместо `lg` (spec требует `lg`) — сознательный trade-off для экономии RAM на 8 GB сервере.
  - LLM fallback (MVP-2) — не реализован.
  - Topics через TF-IDF словарь — не реализован (сейчас только spaCy GPE/LOC/NORP → topic).
  - Тесты для entity-extractor — нет.
  - Allowlist-фильтр в handlers.py telegram-ingestor — всё ещё TODO.
  - B-03 Seed/fixtures скрипты.

- **Принятые предположения.**
  - `sm` модели достаточны для MVP-1 (компромисс точность vs память).
  - `rapidfuzz` ≥ 0.85 хорошо отсекает случайные совпадения — не проверялось на реальных данных.
  - Partition `processed_event_2026_06` и `interaction_2026_06` created заранее (maintenance сервис пока не написан).

- **Известные риски.**
  - `processed_event` partition overflow: партиции только до конца июня 2026. Нужен maintenance service (B-04) который создаёт партиции наперёд. Без него вставки в июле упадут.
  - Entity extractor записывает `mention` только когда оба — speaker и mentioned — уже есть в таблице `person`. Первые взаимодействия не записываются.
  - BOM проблема: файлы написанные через Write tool на Windows имеют UTF-8 BOM. При деплое через scp нужно стриппить BOM. Добавить в `Makefile deploy` шаг `python3 strip_bom.py`.

- **Следующий шаг.**
  - **D-02** persona-builder: consumer `events.processing.entity_found` → агрегация профиля персоны → обновление `person.organizations`, `person.topics`, `person.role`.
  - Параллельно: **B-04** maintenance — создание партиций на 2026-07+ (критично, иначе июльские данные потеряем).

- **Рекомендованный агент.** `claude` — D-02 затрагивает contracts (PersonProfileUpdatedEvent), новый сервис, и Postgres UPDATE.

- **Контекст для следующего агента.**
  Поток: `events.processing.entity_found` (EntityFoundEvent) → persona-builder → UPDATE `person` + публикация `events.processing.person_updated`.
  Смотреть: `libs/contracts/events.py` (EntityFoundEvent, EntityCandidate), `docs/03-data-model/postgres-schema.md` (person.topics, person.organizations, person.role).
  Сервис стартует на `192.168.31.165`, compose в `~/pil/infra/compose/`.

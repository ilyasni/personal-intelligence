[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "task-extractor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pil-contracts",
    "pil-observability",
    "pil-storage",
    "dateparser>=1.2",
    "asyncpg>=0.29",
    "pydantic-settings>=2.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/task_extractor"]
